In artifacts/musicoteca/components/exhibit-view.tsx, 
make the globe toggle also transliterate bolded entity names 
inside the Inner World and The Moment panels.

Do NOT change anything else.

--- CHANGE 1: Update renderMarkdown to accept showTranslit ---

Change the function signature from:
function renderMarkdown(text: string, lang: string)
to:
function renderMarkdown(text: string, lang: string, showTranslit: boolean)

Inside the strong component handler, add transliteration:

strong: ({ children }) => {
  const entityText = String(children ?? "");
  const wikiUrl = `https://${lang}.wikipedia.org/wiki/Special:Search?search=${encodeURIComponent(entityText)}`;
  
  // Transliterate display text if toggle is on and text contains non-Latin
  const isNonLatin = /[\u0400-\u04FF\u0600-\u06FF\u3040-\u9FFF\u0370-\u03FF]/.test(entityText);
  const displayText = showTranslit && isNonLatin 
    ? transliterateRu(entityText)  // reuse existing transliterateRu or a generic one
    : entityText;
  
  return (
    <a href={wikiUrl} target="_blank" rel="noopener noreferrer"
      className="font-bold text-ink underline decoration-warm-grey/40 
      underline-offset-4 transition-colors hover:decoration-ink 
      dark:text-chalk dark:hover:decoration-chalk"
      title={`Search Wikipedia for ${entityText}`}
    >
      {displayText}
    </a>
  );
}

--- CHANGE 2: Add transliterateRu as a client-side utility ---

Since transliterateRu is currently only in the server route, 
add a simple version directly in exhibit-view.tsx:

const RU_TRANSLIT: Record<string, string> = {
  а:"a",б:"b",в:"v",г:"g",д:"d",е:"e",ё:"e",ж:"zh",
  з:"z",и:"i",й:"y",к:"k",л:"l",м:"m",н:"n",о:"o",
  п:"p",р:"r",с:"s",т:"t",у:"u",ф:"f",х:"kh",ц:"ts",
  ч:"ch",ш:"sh",щ:"shch",ъ:"",ы:"y",ь:"",э:"e",ю:"yu",я:"ya",
};

function transliterateRu(text: string): string {
  return text.split("").map((ch) => {
    const lower = ch.toLowerCase();
    const mapped = RU_TRANSLIT[lower];
    if (mapped === undefined) return ch;
    if (mapped === "") return "";
    return ch === lower 
      ? mapped 
      : mapped.charAt(0).toUpperCase() + mapped.slice(1);
  }).join("");
}

Place this near the top of the file, before the component function.

--- CHANGE 3: Pass showTranslit into renderMarkdown calls ---

Find the two renderMarkdown calls in Panel 2 and Panel 3:
{renderMarkdown(innerWorld, lang)}
{renderMarkdown(theMoment, lang)}

Change both to:
{renderMarkdown(innerWorld, lang, showTranslit)}
{renderMarkdown(theMoment, lang, showTranslit)}

--- THAT IS ALL ---
No other changes. No layout changes. No new dependencies.